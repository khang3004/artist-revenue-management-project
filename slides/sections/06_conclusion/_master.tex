% ============================================================
% Section 6: Conclusion
% ============================================================
\section{Conclusion}

\begin{frame}{\secname}
    \sectionCard{Conclusion}{Summary \textbf{·} Technical Highlights \textbf{·} Team Contributions}
\end{frame}

% ----- Frame: Summary -----
\begin{frame}{Summary}
    \begin{columns}[T]
        \begin{column}{0.50\textwidth}
            \begin{exampleblock}{Completed}
                \begin{itemize}\scriptsize
                    \item \textbf{19 tables} with 3 full ISA hierarchies \\
                          (overlapping/partial + 2$\times$ disjoint/total)
                    \item \textbf{8 Stored Procedures} --- ROLLUP, PIVOT (FILTER),
                          Nested subquery in HAVING, LATERAL JOIN,
                          Window (RANK), Correlated subqueries,
                          CTE + ROLLUP, GIN + JSONB
                    \item \textbf{Automated ETL pipeline}: MusicBrainz + Ticketbox
                          $\to$ Transform $\to$ Neon (3,180 revenue logs)
                    \item \textbf{5-page Streamlit dashboard} backed by all 8 SPs
                    \item Materialized Views + GIN / B-Tree / Partial / Expression indexes
                \end{itemize}
            \end{exampleblock}
        \end{column}
        \begin{column}{0.46\textwidth}
            \begin{block}{Technical Highlights}
                \begin{itemize}\scriptsize
                    \item \textbf{Polymorphic FK} --- \tbl{contract\_splits} $\to$
                          Artist XOR Label (ISA beneficiaries table)
                    \item \textbf{UUID PK} for contracts --- prevents ID enumeration
                    \item \textbf{Append-only} \tbl{revenue\_logs} partitioned
                          by quarter --- immutable financial audit trail
                    \item \textbf{JSONB metadata} + GIN index --- flexible
                          genre \& social-link search
                    \item \textbf{Wallet reconciliation} --- difference $\approx$ 0
                          for all 22 artists (audit\_status = OK)
                    \item \textbf{Neon Serverless} --- SSL enforced,
                          SCRAM-SHA-256, connection pooling
                \end{itemize}
            \end{block}
        \end{column}
    \end{columns}
\end{frame}

% ----- Frame: Team -----
\begin{frame}{Team Contributions}
    \centering
    \begin{tabular}{@{}clll@{}}
        \toprule
        & \textbf{Member} & \textbf{Responsibilities} & \textbf{Share} \\
        \midrule
        A & Member A & Requirements spec, ERD, logical design, report & 25\% \\
        B & Member B & DDL scripts, seed data, views, indexes          & 25\% \\
        C & Member C & Stored Procedures (8 SPs), ETL pipeline         & 25\% \\
        D & Member D & Streamlit dashboard (5 pages), slides           & 25\% \\
        \bottomrule
    \end{tabular}

    \vspace{0.8em}
    \begin{columns}[T]
        \begin{column}{0.48\textwidth}
            \begin{block}{Git Workflow}
                \scriptsize
                \texttt{feat/fix/docs} branches \\
                $\to$ \texttt{develop} $\to$ \texttt{main}
            \end{block}
        \end{column}
        \begin{column}{0.48\textwidth}
            \begin{block}{Commit Convention}
                \scriptsize
                \texttt{feat(db): add ISA tables} \\
                \texttt{fix(sp): cast play\_count to BIGINT} \\
                \texttt{docs(erd): update 19-table diagram}
            \end{block}
        \end{column}
    \end{columns}
\end{frame}
