% ============================================================
% Section 5: Demo (Slide 11)
% ============================================================
\section{Demo}

% ----- Slide 11: Demo -----
\begin{frame}{Live Demo}
    \begin{columns}[T]
        \begin{column}{0.50\textwidth}
            \begin{alertblock}{Data Storyteller Opening}
                \centering
                {\large \coral{Khanh Vu Nguyen Nang}} \\
                \vspace{0.3em}
                \scriptsize Mở đầu bằng phần rap intro, \\
                sau đó chuyển sang demo SQL
            \end{alertblock}

            \vspace{0.4em}
            \begin{block}{Kịch bản demo}
                \begin{enumerate}\small
                    \item Đăng ký nghệ sĩ mới (SP9)
                    \item Ghi nhận doanh thu (SP10)
                    \item ROLLUP doanh thu (SP1)
                    \item Pivot theo nguồn (SP2)
                    \item Wallet audit (SP6)
                    \item Tìm kiếm JSONB (SP8)
                \end{enumerate}
            \end{block}
        \end{column}
        \begin{column}{0.46\textwidth}
            \begin{tikzpicture}[
                node distance=0.7cm,
                font=\scriptsize,
                box/.style={draw, rounded corners, minimum width=3.2cm,
                            minimum height=0.55cm, align=center}
            ]
                \node[box, fill=coralAccent!15]  (user)  {Browser};
                \node[box, fill=iceBlue!40,      below=of user]  (st)    {Streamlit (:8501)};
                \node[box, fill=codeGreen!15,    below=of st]    (sp)    {14 Stored Procedures};
                \node[box, fill=navyPrimary!15,  below=of sp]    (db)    {Neon PostgreSQL 16};

                \draw[->, thick] (user) -- (st);
                \draw[->, thick] (st)   -- (sp)
                    node[midway, right] {\texttt{SELECT/CALL}};
                \draw[->, thick] (sp)   -- (db)
                    node[midway, right] {\texttt{RETURN TABLE}};
            \end{tikzpicture}

            \vspace{0.5em}
            \begin{exampleblock}{\small Backup plan}
                \scriptsize
                Nếu Streamlit không ổn định (cold start) \\
                $\to$ demo trực tiếp qua pgAdmin / DBeaver
            \end{exampleblock}
        \end{column}
    \end{columns}

    \note{
        ``Khanh mở đầu bằng rap intro, sau đó chuyển sang demo SQL.''
        Demo live trên Neon --- chạy SP trực tiếp, hiện kết quả.
        Backup: nếu Streamlit cold start quá lâu (Neon free tier),
        chuyển sang pgAdmin chạy SQL trực tiếp.
        Thời gian demo: 3--4 phút.
    }
\end{frame}
