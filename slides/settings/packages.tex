% -----------------------------------------------------------------------------
% File: settings/packages.tex
% Compile: xelatex -shell-escape main.tex
% -----------------------------------------------------------------------------

% --- Visualization ---
\usepackage{tikz}
\usetikzlibrary{shapes, shapes.geometric, arrows, positioning, fit, calc, shadows}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}

% --- Layout & Formatting ---
\usepackage{booktabs}
\usepackage{colortbl}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{multicol}
\usepackage{tabularx}
\usepackage{adjustbox}

% --- Code Highlighting ---
\usepackage{listings}
\lstset{
    language=SQL,
    basicstyle=\ttfamily\tiny,
    keywordstyle=\bfseries\color{navyPrimary},
    stringstyle=\color{codeGreen},
    commentstyle=\color{gray},
    breaklines=true,
    frame=single,
    backgroundcolor=\color{iceBlue!15},
    numbers=left,
    numberstyle=\tiny\color{gray},
    morekeywords={ROLLUP, GROUPING, FILTER, LATERAL, PARTITION, OVER,
                  RANK, JSONB, CALL, PROCEDURE, RETURNS, LANGUAGE,
                  plpgsql, COALESCE, RAISE, EXCEPTION, NOTICE,
                  RETURNING, EXECUTE, FUNCTION, BEGIN, END, DECLARE,
                  RECORD, FOUND, PERFORM, IF, THEN, ELSE, ELSIF,
                  CASE, WHEN, INTO, OUT, INOUT, LOOP, EXIT, FOR,
                  make_date, jsonb_build_object},
}

% --- Graphics ---
\usepackage{graphicx}

% --- Hyperlinks ---
\usepackage{hyperref}
\hypersetup{colorlinks=true, linkcolor=navyPrimary, urlcolor=coralAccent}
